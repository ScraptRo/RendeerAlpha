<?xml version="1.0" encoding="UTF-8"?>
<!-- Reusable interface pieces, instantiated by name from C++.
     Sizes are a number, "fill", "fill:2" (a weight), or "content"; min/max clamp them.
     Edit this file and, in a build with RDA_ENABLE_HOT_RELOAD defined, the running
     application picks the change up without a rebuild.

     Empty for now: ScriptCell and NotebookToolbar lived here to serve the notebook
     demo and went with it. Sandbox no longer loads this file either -- its interface
     comes from a compiled layout (res/layouts/hello.tsx) instead. This is kept as a
     record of the format, which remains available to any application that wants a
     widget subtree it can instantiate by name. -->
<components>

</components>
