{
  "//": "Type checking only. Nothing here emits: esbuild does the transform, and this exists so a typo in a layout is a red squiggle in the editor instead of a warning in the log. Run it with: node_modules/.bin/tsc -p Sandbox/res/layouts",
  "compilerOptions": {
    "noEmit": true,
    "strict": true,
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react",
    "jsxFactory": "h",
    "types": [],
    "skipLibCheck": true
  },
  "include": ["rda.d.ts", "*.tsx"]
}
